services:
  web:
    build: .
    container_name: paloma_api_web_prod
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    command: >
      sh -c "python manage.py migrate &&
             gunicorn core_api.wsgi:application --bind 0.0.0.0:8000 --workers 3"